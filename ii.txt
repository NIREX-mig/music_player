useEffect(() => {
    let timeout;

    const handleTypingStopped = () => {
      // This function will be called after typing stops
      console.log('Typing stopped. Perform your action here.');
    };

    const handleInputChange = (event) => {
      // Update the input value whenever the user types
      setInputValue(event.target.value);

      // Clear the previous timeout
      clearTimeout(timeout);

      // Set a new timeout to determine when typing stops
      timeout = setTimeout(handleTypingStopped, 1000); // Adjust the delay as needed
    };

    // Cleanup function to clear the timeout when the component is unmounted
    return () => clearTimeout(timeout);

    // Add inputValue to the dependency array if you want to perform an action
    // whenever the input value changes
  }, [inputValue]);
